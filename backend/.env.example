# 服务端口
PORT=8787

# SQLite 数据库文件路径（相对 backend 根目录）
DATABASE_URL=./data/app.db

# PDF 本地存储目录
PDF_STORAGE_DIR=./storage/pdfs

# 允许的前端来源（CORS），多个用逗号分隔
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# 日志级别：fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# 环境
NODE_ENV=development

# ========== Hermes Agent 同机 HTTP 直连 ==========
# 详见 Hermes_Command_Center_HTTP_直连可用版.md
# Backend 通过该地址调用 Hermes Agent 的 HTTP 接口。
HERMES_BASE_URL=http://127.0.0.1:8642
# 单次 Hermes 调用的超时（毫秒）。Agent 执行可能较久，默认 120s。
HERMES_TIMEOUT_MS=120000
# 可选：Hermes 若需要 token 鉴权，放这里，会以 Authorization: Bearer <key> 发送。
# HERMES_API_KEY=local-dev-key
